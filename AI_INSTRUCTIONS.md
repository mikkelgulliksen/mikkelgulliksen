## AI / Agent Instructions (IMPORTANT)

This repo uses **uv** for Python deps and venv management.

**YOU MUST DO THIS:**
- Install deps: `uv sync`
- Add deps: `uv add <pkg>`
- Run python: `uv run python ...`
- Run scripts: `uv run <command>`

**YOU MUST NOT DO THIS:**
- Do NOT use `pip install ...`
- Do NOT create/activate venv manually
- Do NOT use `python -m venv` or `source .venv/bin/activate`

Project type: 2  
Project root: /Users/mikkel/Dev/projects/mikkelgulliksen
